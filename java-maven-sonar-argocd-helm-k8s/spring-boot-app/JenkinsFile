pipeline {

    agent any

    stages {

        stage('Checkout code') {

            steps{
            sh "cd java-maven-sonar-argocd-helm-k8s\spring-boot-app" && git branch: 'main', credentialsId: 'github_auth', poll: false, url: 'https://github.com/icicibank-org-nov/Jenkins-Zero-To-Hero.git'
            
            }
        }
    }

}